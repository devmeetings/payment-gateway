extends layout

block content
  
  .container.vertical-padding-s
    .row
      .col-md-8.col-md-offset-2.col-sm-10.col-sm-offset-1
        .vertical-space-l
        .alert.alert-info
          h4.noHeaderMargins
            .pull-left.fa.fa-thumbs-o-up.fa-4x
            | Zarejestrowałeś się na DevMeeting #{claim.event.title}.
          h5
            | Otrzymaliśmy płatność za Twój bilet, a potwierdzenie przesłaliśmy mailem.
        if claim.invoice.invoiceNo
          .well
            p Twoja faktura jest już gotowa, aby ją pobrać proszę podać adres email podany podczas rejestracji.
            form(action="/events/#{claim.event.id}/invoice/#{claim._id}", name="invoiceForm", method="POST")
              .form-group
                input.form-control(type="email", required, name="email", autocomplete="off")
              button.btn.btn-primary(type="submit") Pobierz fakturę
        hr
        h5.text-muted.noHeaderMargins W razie jakichkolwiek problemów skontaktuj się z nami pod adresem <code>registration@devmeetings.org</code>
        h5.text-muted.noHeaderMargins
          | Id twojego biletu: 
          code #{claim._id}
        
        .vertical-space-xl
