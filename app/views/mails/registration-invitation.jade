extends ./mail.jade

block title
  | Możliwość rejestracji na #{event.title}

block content
  p Cześć!
  p Otrzymujesz możliwość rejestracji poza standardową pulą wejściówek na DevMeeting #{event.title}
    | (#{event.city} - #{eventDate}).

  p Link jest ważny przez 48 godzin, do #{endDate}. Rejestracja po tym terminie nie będzie możliwa.

  strong Link do rejestracji:<br/>
    a(
      href="#{appUrl}/events/#{event._id}/tickets/#{claim._id}"
      ) Rejestracja
  p
    | Pozdrawiamy!<br/>
    | Zespół DevMeetings.pl
